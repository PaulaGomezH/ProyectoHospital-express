<table border="solid">
    <h2>LISTADO DE MEDICOS</h2>
    <thead>
        <tr>
            <th>Cédula</th>
            <th>Nombre</th>
            <th>Apellido</th>
            <th>Especialidad</th>
            <th>Consultorio</th>
            <th>Correo</th>
            <th>Opciones</th>
        </tr>
    </thead>
    <tbody>
       {{#each medicos}}
       <tr>
<form action="/medicos/actualizar/{{cedula}}" method="post">


        <td><input id="cedula" name="cedula" value="{{cedula}}" disabled></td>
        <td><input id="medico" name="medico" value="{{nombres}}"{{../opcion}}></td>
        <td><input id="lastname" name="lastname" value="{{apellidos}}"{{../opcion}}></td>
        <td><input id="especialidad" name="especialidad" value="{{especialidad}}"{{../opcion}}></td>
        <td><input id="consultorio" name="consultorio" value="{{consultorio}}"{{../opcion}}></td>
        <td><input id="correo" name="correo" value="{{correo}}"{{../opcion}}></td>
     <td> {{#if ../activo}}
                    <a class="eliminar" href="medicos/eliminar/{{cedula}}">Eliminar</a>
                    <br>
                    <br>
                    <br>
                    <br>
                    <a class="editar" href="/medicos/activar">Editar</a>
                    {{else}}
                    <input type="submit" value="Enviar">
                    {{/if}}
                </td>
            </tr>
        </form>
        {{/each}}
    </tbody>
</table>
<a href="medicos/agregar">Agregar pacientes</a>
<a href="/">Volver</a>